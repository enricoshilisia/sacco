<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: allow plain HTTP to any host (local dev servers,
     the Android emulator's 10.0.2.2). Release builds use src/main's config. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
